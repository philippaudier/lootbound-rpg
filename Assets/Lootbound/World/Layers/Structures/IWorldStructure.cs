namespace Lootbound.World.Layers.Structures
{
    /// <summary>
    /// Placeholder marker for the World Structures layer - the rare, deterministic
    /// graph (Layout, Roads, Bridges, Villages, Landmarks, Shrines) that lands in
    /// T4. Empty on purpose: the folder and the concept exist now so future work
    /// has an obvious home. No implementation in T2.
    /// </summary>
    public interface IWorldStructure
    {
    }
}
